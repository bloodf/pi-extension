# Reference these names from provider-discovery.json. Never commit real values.
GATEWAY_API_KEY=
GATEWAY_TENANT=

# Optional explicit config location.
# PI_PROVIDER_DISCOVERY_CONFIG=/absolute/path/to/provider-discovery.json
